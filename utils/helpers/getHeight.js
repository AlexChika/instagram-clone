/* --------------------------------------- */
/*      Gets The height of the browser     */
/* --------------------------------------- */
const getHeight = () => {
  let height = window.innerHeight;
  return height;
};

export default getHeight;

// the setHeight param is a useState function
